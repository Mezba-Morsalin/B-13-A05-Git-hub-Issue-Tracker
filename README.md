1. What is the difference between var, let, and const?

Ans : var, let, and const are used to declare variables in JavaScript, but they behave differently.
``(i) var is function-scope and because we should redeclare and update var.
  (ii) let is block-scope we can update let and can't redeclare at same scope.
  (iii) const is block-scope we can't update and redeclare after const  assigned.
Because After Launching ES6, modern JavaScript usually suggest let and const replace var.

2. What is the spread operator (...)?

Ans : JavaScript spread operator (...) is use to expand and spread the element of a array or the properties of the object into another array or object. It makes it easy to copy, merge, or work with multiple values.

3. What is the difference between map(), filter(), and forEach()?

Ans : map(), filter(), and forEach() are array methods in JavaScript used to work with array elements.
   (i) map() create a new array to apply a function to every element of the original array.
   (ii) filter() create a new array containing only the element that pass a condition.
   (iii) forEach() runs a function for each element in the array but do not return a new array.

4. What is an arrow function?

Ans : Arrow Function is shorter and easier way to declare a Function. Example : const displayAllCards = () => {}. This is arrow function. its shorter and its applying to use modern JavaScript Es6.

5. What are template literals?

Ans : Temple Literals are a feature in JavaScript it allows to create strings more easy. They use backticks (``) single or double quotes and make simple to add variables and expression inside a strings.